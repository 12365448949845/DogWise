{
  "buildCommand": "cd InspireHub && npm install && npm run build",
  "outputDirectory": "InspireHub/dist",
  "framework": "vite",
  "installCommand": "cd InspireHub && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
